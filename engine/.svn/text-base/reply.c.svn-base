/*************************************************************************
    File Name   :    reply.c
    Author      :    sunzg
    Mail        :    suclinux@gmail.com
    Created Time:    2016年11月10日 星期四 14时13分50秒
*************************************************************************/

#include <stdio.h>

#include "reply.h"

    apply_t *
apply_create(content_t *cn, const char *name, uint32_t reqip)
{
    apply_t *ap = (apply_t *)calloc(sizeof(apply_t), sizeof(char));

    /* Map domain, get type */

    /* Area info, isp */

    /* Get node list */

    /* Check node status */

    /* Need many addrs */

    return ap;
}

    void
apply_free(apply_t *ap)
{
    if (ap) {
        free(ap);
    }
}
